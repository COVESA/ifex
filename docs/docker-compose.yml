services:
  # generate-types:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.generate-types
  #   command: sh -c "python3 generate-types-doc.py > specification/generated-types.md"
  #   volumes:
  #     - .:/app
  #   working_dir: /app

  vitepress:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run docs:dev -- --host 0.0.0.0 --port 5173
    # depends_on:
    #   generate-types:
    #     condition: service_completed_successfully